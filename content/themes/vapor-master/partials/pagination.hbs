<nav class="pagination" role="pagination">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}"><i class="fa fa-chevron-circle-left"></i> Voir les articles plus récents</a>
    {{/if}}
    <span class="page-number">Page {{page}} sur {{pages}}</span>
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Voir les articles plus anciens <i class="fa fa-chevron-circle-right"></i></a>
    {{/if}}
</nav>